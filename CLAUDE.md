# plant-agent

Plant monitoring system: Raspberry Pi + camera captures photos of houseplants, uploads to Azure Blob Storage, web app for browsing photos, logging care actions, and AI-powered plant health insights via Claude.

## Architecture

- `pi/` — Python FastAPI service on Raspberry Pi 5 (Camera Module 3 + Arducam B0283 pan-tilt bracket)
- `frontend/` — Vite + React 19 + TypeScript + Tailwind CSS v4, hosted on Azure Static Web App (plants.romaine.life)
- `backend/` — Node.js Express API, hosted on Azure Container App (plants.api.romaine.life)
- `snapshot/` — SQLite snapshot generator (Node 20). Queries Cosmos DB + Blob Storage, writes `.db` file consumed by the frontend via sql.js/WASM. Runs every 4 hours via GitHub Actions cron
- `tofu/` — OpenTofu infrastructure (resource group, blob storage, Cosmos DB, Container App, Static Web App)
- `enclosure/` — 3D-printable enclosure for Pi 5 + Arducam B0283 pan-tilt camera. Parametric OpenSCAD source files plus a CadQuery Python script (`enclosure_cadquery.py`) that generates STEP files (B-Rep solids) for import into Fusion 360, SolidWorks, FreeCAD, etc.

## Key Decisions

- Pi networking: Cloudflare Tunnel (pi.romaine.life → localhost:8420)
- Photo upload: Backend proxies — Pi returns JPEG bytes, backend uploads to blob via shared managed identity
- Managed identity: User-assigned `infra-shared-identity` from infra-bootstrap, pre-configured with Cosmos, App Config, Key Vault, and Storage roles
- Auth: Microsoft Sign-In via MSAL.js redirect flow with self-signed JWT. Any Microsoft account can log in; owner (`nelson-devops-project@outlook.com`) gets `admin` role, all others get `viewer` role. No login page — nav Sign In button (Microsoft-branded) redirects straight to Microsoft. AuthContext handles MSAL redirect response on any route, prepends `API_BASE` to login POST, exposes `isAdmin` helper. Backend verifies Microsoft ID tokens via JWKS. Microsoft Client ID read from shared plain App Config key (`microsoft_oauth_client_id_plain`). Local dev requires `frontend/.env.local` with `VITE_MICROSOFT_CLIENT_ID` and `VITE_API_BASE`. Public view-only access for browsing plants, photos, events, tasks, and analyses. Admin required for management actions (CRUD, logging events, chat, capture, analysis triggers). Viewers see amber "view-only privileges" banner and write UI is hidden
- Database: Cosmos DB PlantAgentDB in shared infra-cosmos (free tier). Containers: `plants` (/id), `events` (/plantId), `analyses` (/plantId), `chats` (/plantId), `rooms` (/id)
- AI: Claude claude-3-haiku via Anthropic API for plant enrichment (vision-based species ID on creation), analysis, and chat. Model constrained by API key access — currently `claude-3-haiku-20240307`
- Agentic capture: Backend endpoint orchestrates Claude tool-use loop with Pi HTTP API
- Snapshot system: Backend Container App scales to zero causing 30s+ cold starts. A GitHub Actions cron (`snapshot.yml`, every 4 hours) generates a SQLite snapshot from Cosmos DB + Blob Storage and commits it to the repo (`frontend/public/snapshot.db`). Frontend loads it via sql.js (WASM) for anonymous visitors — reads served entirely in-browser with zero backend dependency. Authenticated users discard the snapshot and switch to the live API. Follows the same pattern as kill-me's snapshot system. Key files: `snapshot/generate-snapshot.js` (generator), `frontend/src/api/snapshot.ts` (query functions mirroring API shapes), `frontend/src/api/snapshotContext.tsx` (`SnapshotProvider`, `useDataSource` hook). `staticwebapp.config.json` excludes `/snapshot.db` and `/sql-wasm.wasm` from the SWA navigation fallback so missing files return 404 (not index.html). WASM binary self-hosted via Vite plugin (no CDN). **Critical `useDataSource()` contract:** every consumer MUST check `isReady` before calling any fetch function. The snapshot loads asynchronously (WASM init + network fetch); until ready, `db` is null and `isLive` is true, causing fetches to silently hit the live API (which doesn't exist for anonymous users → permanent loading spinner). Pattern: `const { fetchPlants, isReady } = useDataSource(); useEffect(() => { if (!isReady) return; fetchPlants()... }, [isReady]);`

## Change Log

- **2026-03-14** — Initial scaffold: repo structure, tofu infra, GitHub Actions workflows, Pi FastAPI stubs, backend Express skeleton, frontend React scaffold
- **2026-03-14** — Deep implementations: PlantDetail view (PhotoTimeline, EventLog, LogAction), TaskQueue with backend computation, ChatPanel with Claude SSE streaming, PhotoBrowser, public/auth access split
- **2026-03-14** — Switch Container App to shared user-assigned managed identity (infra-shared-identity) from infra-bootstrap; remove per-app role assignments for Cosmos, App Config, Key Vault, Storage
- **2026-03-14** — Add SWA navigation fallback config (`staticwebapp.config.json`), root `package.json` with `concurrently` for `npm run dev`, backend `.env` for local dev against real Azure resources, gitignore `*.tsbuildinfo` and root `node_modules/`
- **2026-03-14** — Frontend visual overhaul: added Tailwind CSS v4 (`@tailwindcss/vite`), custom "Greenhouse Modern" theme (leaf green + bark neutral palettes, Inter font) in `src/index.css`. Created 7 shared UI primitives in `src/components/ui/` (Card, Badge, Button, Skeleton, EmptyState, ErrorBanner, PageHeader). Replaced all inline `style={{}}` objects with Tailwind utility classes across every component. Added sticky nav with mobile hamburger, active-route highlighting, loading skeletons, proper empty states, responsive grid layouts, chat message bubbles with streaming dots, urgency-colored task cards, and a polished login page.
- **2026-03-14** — Replace local username/password auth with Google Sign-In. Frontend uses Google Identity Services (popup flow) on login page. Backend verifies Google ID token via `google-auth-library`, restricts access to single allowed email, upserts account in Cosmos DB, issues self-signed JWT. Removed `bcryptjs`, `local-routes.js`. Google Client ID stored in Key Vault (`plant-agent-google-client-id`).
- **2026-03-14** — Add dark mode with toggle. Created `ThemeContext` (`src/theme/ThemeContext.tsx`) using class-based dark mode strategy with localStorage persistence, defaulting to dark. Added sun/moon toggle button in navbar. Applied `dark:` Tailwind variants across all 17 component files (AppShell, UI primitives, page components). Configured `@custom-variant dark` in `index.css` for Tailwind v4.
- **2026-03-14** — Rename frontend DNS from `plant.romaine.life` to `plants.romaine.life` (and backend API to `plants.api.romaine.life`). Only the `front_app_dns_name` local in `tofu/frontend.tf` changed; all Azure resource names remain `plant-agent-*`.
- **2026-03-14** — Add `enclosure/` directory with OpenSCAD parametric 3D-printable enclosure. Two-piece design (base + top) housing Pi 5 with port cutouts/vents and a cylindrical turret for Arducam B0283 pan-tilt camera. Includes component library (`lib/`), assembly preview, and README with print settings/BOM/assembly instructions.
- **2026-03-15** — Fix black screen on plants.romaine.life (two issues): (1) `API_BASE` in `frontend/src/api/client.ts` was empty, so production API calls hit the SWA origin instead of `plants.api.romaine.life` — fixed by reading `VITE_API_BASE` env var, added `.env.production` and CI build env. (2) SWA deploy action with `skip_app_build: true` was serving the source `index.html` (referencing `/src/main.tsx`) instead of the built output — fixed by changing `app_location` from `./frontend` to `./frontend/dist`. Also excluded `/api/*` from SWA navigation fallback.
- **2026-03-15** — Switch auth from Google Sign-In to Microsoft via MSAL.js. Frontend: replaced Google Identity Services with `@azure/msal-browser` redirect flow, removed LoginPage component and `/login` route — nav Sign In button calls `loginWithMicrosoft()` directly, AuthContext handles redirect response on any route. Backend: replaced `google-auth-library` with `jwks-rsa` for Microsoft ID token verification via JWKS endpoint, reads Microsoft Client ID from shared plain App Config key (`microsoft_oauth_client_id_plain`) instead of per-app Key Vault secret. CI: `VITE_MICROSOFT_CLIENT_ID` injected from GitHub Actions variable (set by infra-bootstrap). Removed `google-routes.js`, `LoginPage.tsx`, Google type declarations.
- **2026-03-15** — Add view-only mode for non-owner users. Backend: any Microsoft account can now log in (no longer 403 for unknown emails); owner email gets `admin` role, all others get `viewer` role. Added admin-only guards to POST events and POST chat routes. Frontend: `useAuth()` exposes `isAdmin` boolean, AppShell shows amber "view-only privileges" banner for non-admin users, PlantDetail hides LogAction and ChatPanel for viewers.
- **2026-03-15** — Restyle Sign In button to follow Microsoft branding guidelines: white bg with border, four-color Microsoft logo SVG, "Sign in with Microsoft" text, dark mode variant. Added `cursor-pointer` to shared Button component.
- **2026-03-15** — Fix local dev auth: AuthContext login POST now prepends `API_BASE` (was hitting Vite dev server instead of backend). Created `frontend/.env.local` with `VITE_MICROSOFT_CLIENT_ID` and `VITE_API_BASE` for local dev, added to `.gitignore`. Fixed `APP_CONFIG_PREFIX` in `backend/.env` from `plant` to `plants` (keys in App Config use plural prefix). Changed admin account from `fullnelsongrip@gmail.com` to `nelson-devops-project@outlook.com`.
- **2026-03-15** — Wire up plant deletion UI. Created reusable `ConfirmDialog` component (`frontend/src/components/ui/ConfirmDialog.tsx`) with backdrop, Escape key dismiss, and loading state. Added admin-only "Delete" button to PlantDetail hero section that opens confirmation dialog, calls existing `deletePlant()` API, and navigates to `/` on success. Backend endpoint and frontend API function already existed — this change only adds the UI.
- **2026-03-15** — Add plant creation form with photo upload and Claude vision enrichment. New `AddPlantForm` component in PlantGrid (admin-only "+ Add Plant" button). Form accepts name, notes, and optional photo (file or URL). `POST /api/plants` now accepts multipart form data: uploads photo to Azure Blob Storage, sends it to Claude via vision API to identify species and generate care notes (`claudeNotes`), then creates plant in Cosmos DB — all in a single request. Enrichment errors halt creation and surface to the user. Added `claudeNotes` field to Plant type. PlantDetail hero now displays species, claudeNotes, and conditionally hides empty room/position line.
- **2026-03-15** — Implement photo routes. `GET /api/plants/:plantId/photos` lists blobs by plant slug prefix from Azure Blob Storage. `POST /api/plants/:plantId/photos` accepts file upload (multer) or URL fetch, stores as `{slug}/{timestamp}.{ext}`. Backend passes `plantsContainer` to photo routes for slug lookup. Fixed `usePhotos` hook to use `createdAt` field (was `date`, causing "Invalid Date" display).
- **2026-03-15** — Add 503 retry with exponential backoff to `apiFetch` client (3 attempts) and auth login flow, handling backend cold starts when Container App scales from zero (`min_replicas=0`).
- **2026-03-15** — Remove dead `/api/species-lookup` route from chat.js (moved to plant creation enrichment). Switch Claude model from `claude-sonnet-4-6-20250514` to `claude-3-haiku-20240307` across all routes (API key lacks Sonnet access).
- **2026-03-15** — Add click-to-enlarge lightbox for plant photos. Created reusable `PhotoLightbox` component (`frontend/src/components/ui/PhotoLightbox.tsx`) — full-viewport overlay with dark backdrop, Escape/click-outside dismiss. Wired into both `PhotoBrowser` (gallery page) and `PhotoTimeline` (plant detail page) via `selectedPhoto` state. No external library — follows existing `ConfirmDialog` modal pattern.
- **2026-03-15** — Show plant photos as card thumbnails instead of placeholder icon. Added `thumbnailUrl` field to `Plant` type and Cosmos DB document. `POST /api/plants` sets `thumbnailUrl` when created with a photo; `POST /api/plants/:plantId/photos` backfills it on first photo upload for existing plants. `PlantGrid` renders `<img>` with `object-cover` when `thumbnailUrl` exists, falls back to `LeafIcon`. One-time backfill script (`backend/scripts/backfill-thumbnails.js`) patches existing plants from blob storage.
- **2026-03-15** — Fix chat 500 error in production: `ChatPanel` used raw `fetch()` with relative URL, hitting SWA origin instead of backend. Prepended `API_BASE` to chat fetch URL (same fix pattern as earlier `apiFetch` client fix). Exported `API_BASE` from `client.ts`.
- **2026-03-15** — Include recent photos in chat context. Chat route now fetches the 3 most recent photos from Azure Blob Storage and sends them as vision content in the first Claude API call. Photos only attach on the first message (not resent on follow-ups) to control costs. System prompt updated to instruct Claude to assess visual health when photos are present.
- **2026-03-15** — Convert chat to single-turn with persistent history. Each chat is now one question → one answer, saved to new `chats` Cosmos DB container (partitioned by `plantId`). Routes changed from `/api/plants/:plantId/chat` to `/chats` (GET lists history, POST streams single response and saves). Frontend `ChatPanel` shows saved chat history as collapsible cards (question + date, click to expand response), with input at top and "Previous chats" list below. Added `Chat` type, `useChats` hook, `chats` API module. Photos always included (every message is first message). Removed `history` parameter from POST body.
- **2026-03-15** — Reduce backend startup log noise for local dev. Consolidated 4 startup log lines (`Listening...`, `[appConfig] loaded...`, `Connected to Cosmos DB`, `Server ready`) into a single `Server ready on port 3000`. Moved `startServer()` call inside `app.listen()` callback. Added `--watch-preserve-output` to backend dev script so `node --watch` restarts don't clear the terminal (preserving Vite's URL output). Renamed concurrently web prefix to `web:5173` for port visibility.
- **2026-03-17** — Increased snapshot frequency from daily to every 4 hours — changed `snapshot.yml` cron from `0 6 * * *` to `0 */4 * * *` to keep anonymous visitor data fresher.
- **2026-03-16** — Replace top horizontal navbar with left sidebar tab navigation (`TabBar.tsx`), matching the pattern used in eight-queens, bender-world, and kill-me. Uses React Router `Link` components (not state-based switching) because plant-agent has parameterized routes like `/plants/:id`. AppShell rewritten: compact h-12 header (brand + auth) → flex row (sidebar + scrollable content). Mobile: sidebar hidden below md, hamburger dropdown for nav/auth/theme.
- **2026-03-16** — Snapshot workflow now commits `snapshot.db` to the repo instead of only deploying it. This means local dev and CI builds include the snapshot via `git pull` without needing Azure credentials. Removed `snapshot.db` from `.gitignore`. Added `frontend/.vite/` to `.gitignore`. Committed missing `snapshot/package-lock.json` (was breaking `npm ci` in CI). Switched Cosmos endpoint lookup from tofu outputs to App Config (`az appconfig kv show`), matching kill-me's pattern. Added `AZURE_APP_CONFIG_ENDPOINT` GitHub Actions variable.
- **2026-03-16** — Centralized Cosmos DB Data Reader RBAC in infra-bootstrap: added `azurerm_cosmosdb_sql_role_assignment` to the app module so all 5 repo service principals get the role automatically. Removed the previous manual assignment for kill-me's SP.
- **2026-03-16** — Documented the critical `isReady` contract for `useDataSource()` in code comments (snapshotContext.tsx), CLAUDE.md key decisions, and persistent memory. This was prompted by a bug in kill-me where three components missed the guard after porting the snapshot pattern.
- **2026-03-20** — Add STEP file export for the enclosure. OpenSCAD can only export mesh formats (STL), so added a CadQuery Python script (`enclosure/enclosure_cadquery.py`) that mirrors the full OpenSCAD design and exports proper B-Rep STEP files. Supports individual part or full assembly export. Pre-generated `step/base.step`, `step/top.step`, `step/assembly.step`. Updated enclosure README with prerequisites and usage instructions.
- **2026-03-15** — Add static SQLite snapshot system for anonymous visitors (matching kill-me's pattern). Backend Container App scales to zero causing 30s+ cold starts — snapshot pipeline eliminates this for read-only visitors. New `snapshot/` directory: `generate-snapshot.js` queries all 4 Cosmos containers (plants, events, analyses, chats) + Azure Blob Storage (photos), computes care tasks (mirroring `backend/routes/tasks.js` logic), and writes a SQLite `.db` file. Frontend: added `sql.js` dependency with self-hosted WASM binary (Vite plugin copies `sql-wasm.wasm` to `public/`). New `snapshot.ts` provides pure query functions returning identical shapes to the API. New `snapshotContext.tsx` with `SnapshotProvider` and `useDataSource()` hook that transparently routes reads to snapshot (anonymous) or live API (authenticated). Updated all 4 hooks (`usePlants`, `useEvents`, `usePhotos`, `useChats`) and 2 components (`TaskQueue`, `PhotoBrowser`) to read from `useDataSource()` instead of `apiFetch` directly. Write operations remain live API only. `SnapshotProvider` wraps routes inside `AuthProvider` in `App.tsx`. Added `snapshot.yml` workflow (daily 6 AM UTC cron + manual dispatch) that generates the snapshot, builds the frontend with it baked in, and deploys to SWA. Added `cosmos_db_endpoint` tofu output for the workflow. Updated `staticwebapp.config.json` with `.db`/`.wasm` MIME types and excluded `/snapshot.db` + `/sql-wasm.wasm` from the navigation fallback to prevent SWA from serving `index.html` for missing static assets (which caused a "file is not a database" error before the first snapshot run). Added validation query after opening the snapshot DB as a safety net. CI prerequisite: the GitHub Actions OIDC service principal needs Cosmos DB Built-in Data Reader (provisioned by infra-bootstrap app module) and Storage Blob Data Reader roles.
