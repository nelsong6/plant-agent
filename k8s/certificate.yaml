apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: plant-agent-tls
  namespace: plant-agent
spec:
  secretName: plant-agent-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - plants.romaine.life
