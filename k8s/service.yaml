apiVersion: v1
kind: Service
metadata:
  name: plant-agent
  namespace: plant-agent
  labels:
    app: plant-agent
spec:
  type: ClusterIP
  selector:
    app: plant-agent
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
